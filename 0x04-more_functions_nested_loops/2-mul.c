#include "main.h"
/**
 * mul - function that multiplies two integer
 * @a: mulipleir
 * @b: second multipler
 * Return: the result mul
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
